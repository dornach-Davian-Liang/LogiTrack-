# LogiTrack Pro — 系统功能演示报告

> **版本**：2026年3月  
> **适用人员**：业务管理人员、销售人员、定价管理员  
> **系统地址**：http://localhost:3000（内网访问）

---

## 目录

1. [系统概述](#1-系统概述)
2. [登录与用户权限](#2-登录与用户权限)
3. [首页仪表板（Dashboard）](#3-首页仪表板dashboard)
4. [询价管理](#4-询价管理)
   - 4.1 询价列表
   - 4.2 新建询价
   - 4.3 编辑询价
   - 4.4 查看询价详情
   - 4.5 复制询价（Copy）
   - 4.6 递增询价（Increase）
   - 4.7 删除询价
5. [报价管理（Offer）](#5-报价管理offer)
6. [报表中心](#6-报表中心)
   - 6.1 标准报表仪表板
   - 6.2 增强报表（带过滤）
   - 6.3 时期对比报告
7. [主数据管理](#7-主数据管理)
   - 7.1 国家管理
   - 7.2 港口管理
   - 7.3 销售人员管理
   - 7.4 集装箱类型管理
8. [系统设置（仅管理员）](#8-系统设置仅管理员)
   - 8.1 用户管理
   - 8.2 操作日志（审计日志）
9. [常见业务场景演示](#9-常见业务场景演示)
10. [名词解释](#10-名词解释)

---

## 1. 系统概述

**LogiTrack Pro** 是一套专为国际物流询报价业务设计的管理系统，主要服务于中国定价团队（CN Pricing Team）与各地销售团队之间的业务协作。

### 核心价值

| 解决的问题 | 系统提供的功能 |
|-----------|--------------|
| 询价记录散落在邮件、Excel中，难以追踪 | 统一的询价管理台账，一条记录一个业务 |
| 无法快速掌握当月询价转化情况 | 实时报表仪表板，转化率一目了然 |
| 各办事处数据互不相通 | 多办公室数据汇总，支持按CN办事处分析 |
| 操作记录无法追溯 | 审计日志全程记录每一步操作 |
| 重复业务需要从头填写 | Copy / Increase 功能快速复用历史记录 |

### 数据规模

- **询价记录**：14,039 条（历史导入）
- **报价记录**：13,172 条
- **覆盖国家/地区**：206 个
- **港口数据**：1,269 个
- **支持产品类型**：SEA（海运）、AIR（空运）、RAIL-SEA（铁海联运）、AIR-RAIL-SEA（空铁海联运）等 7 种

---

## 2. 登录与用户权限

### 2.1 如何登录

1. 打开浏览器，访问 **http://localhost:3000**
2. 在登录页面输入**用户名**和**密码**
3. 点击 **Login** 按钮

登录成功后，系统会自动跳转到首页仪表板，并在右上角显示当前登录用户名。注销时点击右上角用户名旁边的 **退出** 按钮。

### 2.2 角色权限对照表

系统设有 **3 个角色**，不同角色能看到和操作的功能不同：

| 功能模块 | 普通查看用户<br>NORMAL_USER | 业务操作员<br>OPERATING_USER | 系统管理员<br>ADMIN_USER |
|---------|:-------:|:-------:|:-------:|
| 查看询价列表 | ✅ | ✅ | ✅ |
| 查看询价详情 | ✅ | ✅ | ✅ |
| 新建 / 编辑 / 删除询价 | ❌ | ✅ | ✅ |
| 复制 / 递增询价 | ❌ | ✅ | ✅ |
| 查看报表 | ❌ | ✅ | ✅ |
| 主数据管理（国家/港口等） | ❌ | ✅ | ✅ |
| 用户管理 | ❌ | ❌ | ✅ |
| 审计日志查看 | ❌ | ❌ | ✅ |

> **提示**：如果您发现某些菜单不可见，是因为您的账号权限不足，请联系管理员调整。

---

## 3. 首页仪表板（Dashboard）

登录后默认进入**首页（Dashboard）**，这是了解系统整体状态的入口。

### 3.1 首页显示内容

**统计卡片区（页面顶部4个卡片）**

| 卡片名称 | 含义 |
|---------|-----|
| **Total Enquiries（总询价数）** | 当前选定月份收到的询价总量 |
| **Quoted（已报价）** | 已成功发出至少一次报价的询价数量 |
| **Pending（待处理）** | 尚未确认预订结果的询价数量 |
| **Confirmed（已确认）** | 客户已确认预订的询价数量，转化率显示在卡片下方 |

**状态分布图（Status Breakdown）**

横向进度条展示各状态占总量的百分比，方便一眼判断业务进展比例。

**月度趋势（Monthly Trend）**

显示最近 12 个月每月询价量的变化，并显示较上月的增减幅度（+/- 百分比）。

**地区分布（Top Countries / Ports）**

- 目的国家排名前列
- 起运港（POL）排名前列  
- 目的港（POD）排名前列
- 运输类型占比

**月份切换**

点击页面顶部的月份选择器，可切换查看不同月份的统计数据。

---

## 4. 询价管理

询价管理是系统的**核心业务模块**，点击左侧边栏的 **Enquiries（询价）** 进入。

### 4.1 询价列表

进入询价列表后，可以看到所有询价记录的汇总表格。

**搜索与筛选功能**

| 筛选方式 | 操作方法 | 典型用途 |
|---------|---------|---------|
| 关键字搜索 | 在顶部搜索框输入文字 | 按参考编号（Reference Number）快速定位，如输入 `CN2602` |
| 按状态筛选 | 下拉框选择 New / Quoted / Cancelled | 查找所有尚未报价的新询价 |
| 按产品筛选 | 下拉框选择 SEA / AIR 等 | 只看海运业务 |
| 联合筛选 | 同时设置多个条件 | 查看本月所有状态为"New"的空运询价 |

**列表中每条记录显示的信息**

- 参考编号（Reference Number）：系统自动生成的唯一编号，格式如 `CN2602013-S`
- 产品类型：SEA / AIR 等
- 状态：New（新）/ Quoted（已报价）/ Cancelled（已取消）
- 询价日期
- 操作按钮：查看详情、编辑、复制、递增、删除

**分页导航**

每页显示固定数量的记录，使用底部的翻页按钮切换页码。

---

### 4.2 新建询价

点击列表页顶部的 **+ New Enquiry** 按钮，打开新建询价表单。

表单分为 **9 个信息区块（折叠面板）**，填写时可展开/收起各区块：

#### 区块 1：基本信息（Basic Information）

| 字段 | 说明 | 是否必填 |
|-----|-----|:-------:|
| **Enquiry Received Date（询价收到日期）** | 默认为今天，可修改为实际收到询价的日期 | ✅ |
| **Issue Date（创建日期）** | 默认为今天，**不可手动修改**（系统用此日期生成编号） | ✅ |
| **Reference Number（参考编号）** | **系统自动生成**，无需手填。格式：`国家代码 + 年月 + 序号 + 产品缩写`，例如 `CN2602013-S` 表示中国2026年2月第13条海运询价 | 自动 |
| **Product（产品类型）** | 从下拉框选择：SEA（海运）、AIR（空运）、RAIL-SEA（铁海联运）等 | ✅ |

> **说明**：参考编号由系统根据"创建日期"和"产品类型"自动计算，选完这两项后编号预览会实时更新。

#### 区块 2：人员信息（Personnel）

| 字段 | 说明 |
|-----|-----|
| **CN Pricing Admin（中国定价管理员）** | 负责处理此询价的定价人员，从列表选择 |
| **Sales Country（销售国家）** | 客户所在国家 |
| **Sales Office（销售办公室）** | 负责此客户的销售办公室 |
| **Sales PIC（销售负责人）** | 具体负责的销售人员，从销售主数据中选择 |
| **Assigned CN Office（负责的中国办事处）** | 处理此询价的中国办事处（如上海、深圳等） |

#### 区块 3：货物信息（Cargo Information）

| 字段 | 说明 |
|-----|-----|
| **Cargo Type（货物类型）** | FCL（整箱）/ LCL（拼箱）/ AIR（空运）等 |
| **Volume (CBM)（体积）** | 货物体积，单位：立方米 |
| **Quantity（数量）** | 货物数量 |
| **Quantity Unit（数量单位）** | KG / CBM / PCS / TEU 等 |
| **TEU（标准集装箱当量）** | **系统根据集装箱类型自动计算**，1个20英尺柜=1TEU，1个40英尺柜=2TEU |
| **Commodity（商品名称）** | 货物品名，如 "Electronics / 电子产品" |
| **HAZ / Special Equipment（危险品/特殊设备）** | 是否有危险品或特殊要求，填写说明 |

#### 区块 4：集装箱明细（Container Lines）

针对 FCL（整箱）业务，可添加多条箱型明细：

- 点击 **+ Add Container Line** 新增一行
- 从下拉框选择箱型（20GP / 40GP / 40HQ / 45HQ 等）
- 输入数量
- 系统自动计算 TEU 合计

#### 区块 5：路线信息（Route Information）

| 字段 | 说明 |
|-----|-----|
| **POL（起运港）** | 支持多选。输入港口名称或代码搜索，可选择多个起运港 |
| **POD（目的港）** | 支持多选。同上 |
| **POD Country（目的国家）** | 根据所选目的港自动关联 |

> **说明**：POL 和 POD 均支持**多港口选择**，适合询价涉及多个装卸港口的情况（如询问从上海或宁波发往鹿特丹或汉堡的价格）。

#### 区块 6：报价信息（Offer Information）

见 **[第5章：报价管理](#5-报价管理offer)**

#### 区块 7：分类信息（Classification）

| 字段 | 说明 |
|-----|-----|
| **Core / Non-Core（核心/非核心）** | 是否为核心客户业务，影响报表统计分类 |
| **Category（类别）** | 业务分类（如 Spot / Contract 等） |
| **Cargo Ready Date（货物准备日期）** | 货物预计可以装船的日期 |

#### 区块 8：备注（Remarks）

- **Additional Requirement（附加要求）**：特殊运输要求、有效期等说明，最多 2000 字
- **Remark（内部备注）**：内部沟通备注，不对外展示

#### 区块 9：状态与结果（Status & Result）

| 字段 | 说明 |
|-----|-----|
| **Status（状态）** | New（新建）→ 手动或自动切换为 Quoted（已报价）→ Cancelled（取消）。**当有报价（Offer）时，状态自动变为 Quoted** |
| **Booking Confirmed（预订确认）** | Pending（待定）/ Yes（已确认）/ Rejected（被拒绝）/ Invalid（无效） |
| **Rejected Reason（拒绝原因）** | 当 Booking Confirmed 选择 Rejected 时填写 |
| **Actual Reason（实际原因）** | 编辑已有记录时可填写最终结果原因 |

**保存操作**

填写完成后，点击底部的 **Save Enquiry** 按钮保存。系统会自动校验必填字段并提示错误。

---

### 4.3 编辑询价

在列表中找到需要修改的记录，点击 **编辑（蓝色铅笔图标）** 按钮，打开与新建相同的表单，所有字段均已回填，可直接修改后保存。

---

### 4.4 查看询价详情

点击列表中**眼睛图标**或点击参考编号，进入询价详情页。

详情页展示完整信息，包括：
- 所有基础信息字段
- 集装箱明细列表及 TEU 合计
- 历史报价记录（Offer History）
- 记录创建时间和最后更新时间

---

### 4.5 复制询价（Copy）

**使用场景**：客户发来的新询价与历史某条询价内容高度相似（路线、货物相同），需要快速复用。

**操作步骤**：
1. 在列表中找到要复制的记录
2. 点击**绿色复制图标（Copy）**
3. 系统会打开一个**新的询价表单**，其中已预填了原记录的所有内容（路线、货物、人员等）
4. 修改需要变更的字段（如询价日期、货量）
5. 点击保存，系统自动生成**新的参考编号**

> **注意**：复制出来的是全新的询价，参考编号由系统重新分配，原记录的报价（Offer）不会被复制。状态重置为 **New**。

---

### 4.6 递增询价（Increase）

**使用场景**：针对同一条业务线，客户重新询价（例如上次询了4月份的价格，现在再询5月份的），需要保留原始参考编号的关联性，只递增末尾序号。

**操作步骤**：
1. 在列表中找到原始记录
2. 点击**青色递增图标（Increase）**
3. 系统打开新表单，预填所有原始信息
4. 参考编号**自动递增序号**，例如原来是 `CN2602013-S`，新的会是 `CN2602013-S-2`
5. 修改需要变更的字段（如日期）后保存

> **与 Copy 的区别**：Increase（递增）保留了与原始询价的关联，参考编号有序递增；Copy（复制）生成完全独立的新编号。

---

### 4.7 删除询价

点击列表中**红色垃圾桶图标**，系统会弹出确认提示，确认后删除该条询价记录（**此操作不可撤销**）。

> **提示**：删除操作会被记录到审计日志中，管理员可查询删除历史。

---

## 5. 报价管理（Offer）

报价信息**内嵌在询价表单中**，通过"Offer Information"区块管理。

### 5.1 添加报价

在询价表单的 **Offer Information** 区块：

1. 点击 **+ Add Offer** 按钮
2. 填写以下信息：

| 字段 | 说明 |
|-----|-----|
| **Offer Type（报价类型）** | OCEAN（海运）/ AIR（空运） |
| **Sequence No（序号）** | 报价序号，同一询价第一次报价为 1，第二次修改为 2，以此类推 |
| **Is Latest（是否最新）** | 标记该报价是否为最新的有效报价 |
| **Sent Date（发送日期）** | 向客户/销售发出报价的日期 |
| **Price（价格）** | 报价金额 |
| **Is Rejected Price（是否被拒绝）** | 该报价是否已被客户拒绝 |

3. 一条询价可以有**多条报价记录**（追踪价格变动历史）
4. 点击报价行右侧的**垃圾桶图标**可删除单条报价

### 5.2 状态自动联动

当询价中**存在至少一条报价**时，询价的状态字段会**自动变为 Quoted**（已报价），无需手动修改。若删除所有报价，状态可手动恢复为 New。

---

## 6. 报表中心

点击左侧菜单的 **Reports（报表）** 展开报表子菜单，包含 3 个报表功能：

### 6.1 标准报表仪表板（Report - Dashboard）

这是面向**月度业务回顾**的标准报表。

**功能说明**：
- 顶部选择要查看的**月份**（YYYY-MM 格式）
- 展示所选月份的 4 项核心指标：总询价、已报价、待处理、已确认
- 各指标旁显示**较上月的变化量**（+ 增加 / - 减少）
- 状态分布进度条：直观展示 New / Quoted / Cancelled 各状态占比
- 月度趋势折线（最近 12 个月）
- 目的国家 TOP 排行
- 起运港 / 目的港 TOP 排行
- 货物类型占比

**典型用途**：每月末总结当月询价业务量及转化情况。

---

### 6.2 增强报表（Report - Enhanced）

这是支持**自定义过滤**的高级报表，提供更灵活的数据分析能力。

**新增过滤条件**：

| 过滤项 | 说明 |
|-------|-----|
| **日期区间（Start Date / End Date）** | 可查询任意日期范围，不限于单月（如查询整个季度） |
| **Core Status（核心状态）** | 选择只看 CORE（核心）或 NON-CORE（非核心）业务 |
| **CN Office（中国办事处）** | 只看某个特定办事处的数据（如只看上海办 Shanghai） |

**增强报表专有功能**：

**CN 办事处数据透视表（CN Office Pivot Table）**

以表格形式显示每个中国办事处的询价情况，列包括：

| 列名 | 含义 |
|-----|-----|
| **CN Office** | 办事处名称 |
| **Total** | 总询价数 |
| **Yes（已确认）** | 客户已确认预订的数量 |
| **Rejected（被拒绝）** | 被客户拒绝的数量 |
| **Invalid（无效）** | 无效询价数量 |
| **Pending（待定）** | 尚未有结果的询价数量 |
| **Conversion Rate（转化率）** | `已确认 / 已报价 × 100%` |

点击表格中某一行的数量，可以**直接弹出该办事处对应状态下的询价列表**，支持快速跳转查看和编辑详情。

---

### 6.3 时期对比报告（Report - Comparison）

**使用场景**：对比不同月份或不同季度之间的业务表现，判断增长趋势。

**操作步骤**：

1. 选择**对比类型**：Monthly（月度对比）或 Quarterly（季度对比）
2. 勾选要对比的**时期**（最多选 6 个）
   - 月度：如选择 2026-01、2026-02、2026-03
   - 季度：如选择 2025-Q4、2026-Q1
3. 点击 **Compare（对比）** 按钮

**结果展示**：

- **数据对比表格**：每行为一个时期，显示询价数、已报价数、已确认数、转化率及较上期增长率
- **趋势可视化图表**（TrendChart）包含多种图表视图：
  - **折线图**：显示各时期总询价量的趋势走势
  - **增长率柱状图**：展示环比增长率（正值=增长，负值=下降）
  - **雷达图**：多维度对比各时期在询价总量、已报价、已确认、转化率四个维度上的表现
  - **排名与趋势摘要面板**：高亮显示业绩最佳时期和同/环比趋势方向（上升↑/下降↓/稳定→）

---

## 7. 主数据管理

**主数据**是系统中所有下拉框和关联选择的数据来源。点击左侧菜单 **Master Data（主数据）** 展开：

### 7.1 国家管理（Countries）

- 查看全部国家/地区列表
- 支持搜索
- 可新增、编辑、删除国家记录（需 OPERATING_USER 权限以上）
- 字段包括：国家代码（ISO 标准）、中文名、英文名

### 7.2 港口管理（Ports）

- 查看 1,269 个港口记录
- 支持按港口名称、港口代码、国家搜索
- 字段包括：港口代码（LOCODE）、港口英文名、所在国家
- 港口数据用于询价中 POL（起运港）和 POD（目的港）的选择

### 7.3 销售人员管理（Sales PICs）

- 管理出现在询价"Sales PIC"字段中的人员名单
- 与销售办公室（Sales Office）关联
- 字段包括：姓名、所属国家、所属销售办公室、联系方式

### 7.4 集装箱类型管理（Container Types）

- 管理系统中支持的箱型及其 TEU 折算系数

| 箱型 | TEU 折算 |
|-----|---------|
| 20GP（20英尺普柜） | 1 TEU |
| 40GP（40英尺普柜） | 2 TEU |
| 40HQ（40英尺高柜） | 2 TEU |
| 45HQ（45英尺高柜） | 2.25 TEU |

系统根据箱型和数量**自动计算总 TEU**，无需手动换算。

---

## 8. 系统设置（仅管理员）

此菜单**仅 ADMIN_USER 角色可见**，普通操作员看不到 Settings 菜单。

### 8.1 用户管理（User Management）

- 查看系统所有用户账号列表
- 可**新增用户**：填写用户名、姓名、密码、分配角色
- 可**修改用户信息**：修改联系方式、角色权限、启用/禁用账号
- 可**停用账号**：停用后该用户无法登录（数据不删除，保留历史记录）
- 角色可从 ADMIN_USER / OPERATING_USER / NORMAL_USER 中选择

### 8.2 操作日志（Audit Log）

操作日志记录系统中**每一项关键操作**，用于合规审计和问题追溯。

**日志内容包括**：

| 字段 | 含义 |
|-----|-----|
| **操作时间** | 精确到秒的操作时间戳 |
| **用户名** | 操作人的账号名 |
| **用户角色** | 操作人的权限级别（Admin / 定价管理员 / 普通用户） |
| **CN Pricing Admin** | 被操作询价关联的定价管理员 |
| **操作类型** | 新增 / 修改 / 删除 / 查看 / 导出 / 登录 / 注销 |
| **资源类型** | 被操作的对象类型（询价 ENQUIRY / 用户 USER 等） |
| **资源名称** | 被操作记录的名称或参考编号 |
| **变更摘要** | 修改了哪些字段、改了什么内容，格式如：`status: New → Quoted` |
| **操作状态** | SUCCESS（成功）/ FAILED（失败） |

**筛选功能**：可按时间范围、用户名、操作类型、资源类型进行过滤。

**导出功能**：点击"导出"按钮，可将日志导出为 Excel 文件（`.xlsx`）。

**清空功能**（谨慎使用）：点击"清空"后需二次确认，清空后**无法恢复**。

---

## 9. 常见业务场景演示

### 场景一：收到新询价，完整录入流程

**背景**：德国汉堡的销售团队发来一条询价，需要从上海到鹿特丹运送电子产品。

**操作步骤**：
1. 点击 **Enquiries → + New Enquiry**
2. 询价收到日期选今天
3. 产品选择 **SEA（海运）**
4. CN Pricing Admin 选择自己的名字
5. Sales Country 选 **Germany（德国）**，Sales Office 选对应办事处
6. Cargo Type 选 **FCL（整箱）**
7. 在 Container Lines 添加：**40HQ × 2**（系统自动算出 TEU = 4）
8. POL 搜索选择 **CNSHA（上海）**，POD 搜索选择 **NLRTM（鹿特丹）**
9. POD Country 自动显示 Netherlands
10. Commodity 填写 **Electronics**
11. 保存 → 参考编号自动生成（如 `CN2603025-S`）

---

### 场景二：已报价，更新报价信息

**背景**：上面那条询价已经向客户发了报价，需要记录报价信息。

**操作步骤**：
1. 在列表找到 `CN2603025-S`，点击编辑
2. 在 **Offer Information** 区块点击 **+ Add Offer**
3. Offer Type 选 **OCEAN**，Sequence No 填 **1**，Is Latest 勾选
4. Sent Date 选今天，Price 填报价金额（如 1200）
5. 保存 → 状态自动变为 **Quoted**（已报价）

---

### 场景三：查看本月上海办事处的转化率

**操作步骤**：
1. 点击 **Reports → Enhanced Report（增强报表）**
2. 在过滤条件处：Start Date 填本月1日，End Date 填今天
3. CN Office 选 **Shanghai（上海）**
4. 点击 **Apply Filters**
5. 查看 CN Office Pivot Table 中上海行的 **Conversion Rate** 列

---

### 场景四：对比第一季度和第二季度业务量

**操作步骤**：
1. 点击 **Reports → Comparison Report（对比报告）**
2. Comparison Type 选 **Quarterly（季度）**
3. 勾选 **2026-Q1** 和 **2026-Q2**
4. 点击 **Compare**
5. 查看表格中各季度数据对比，以及下方趋势图的增长率

---

### 场景五：客户再次询问同一条路线

**背景**：客户上个月询过从上海到鹿特丹的价格（`CN2602025-S`），本月再次来询，内容基本相同。

**操作方案 A（使用 Copy）**：
- 适合：这是一条**全新的独立询价**，与上次无关
- 操作：找到 `CN2602025-S`，点击**绿色 Copy 图标** → 修改日期 → 保存
- 结果：生成全新编号如 `CN2603031-S`

**操作方案 B（使用 Increase）**：
- 适合：与上次是**同一条业务线**，是对同一件事的持续跟进
- 操作：找到 `CN2602025-S`，点击**青色 Increase 图标** → 确认内容 → 保存
- 结果：生成递增编号如 `CN2602025-S-2`，保留业务关联性

---

## 10. 名词解释

| 术语 | 全称/含义 |
|-----|---------|
| **Enquiry（询价）** | 客户向销售团队咨询运费的请求，系统中每条询价为一个业务单元 |
| **Offer（报价）** | 定价团队针对询价给出的运费报价 |
| **Reference Number（参考编号）** | 系统自动生成的唯一业务编号，格式：`CN + 年月 + 序号 + 产品缩写` |
| **POL（Port of Loading）** | 起运港，货物装船的港口 |
| **POD（Port of Discharge）** | 目的港，货物卸船的港口 |
| **TEU** | Twenty-foot Equivalent Unit，标准集装箱当量单位，以20英尺集装箱为基准 |
| **FCL（Full Container Load）** | 整箱运输 |
| **LCL（Less than Container Load）** | 拼箱运输 |
| **CORE / NON CORE** | 业务分类标识，表示是否为核心客户/核心业务 |
| **CN Office** | 中国办事处，如上海、深圳、北京等 |
| **Booking Confirmed** | 预订确认状态：Pending（待定）/ Yes（已确认）/ Rejected（被拒）/ Invalid（无效） |
| **Conversion Rate（转化率）** | 已确认预订数 ÷ 已报价数 × 100%，衡量报价成功率 |
| **RBAC** | Role-Based Access Control，基于角色的权限控制 |
| **Audit Log（审计日志）** | 系统操作记录，追踪每一次数据新增、修改、删除的完整历史 |
| **LOCODE** | 联合国港口代码，如 CNSHA = 中国上海，NLRTM = 荷兰鹿特丹 |

---

*本报告由系统文档自动整理生成，最后更新：2026年3月*
